---
import type { ComponentProps } from 'astro/types';
import type { WithAttributes } from '@/types';
import ButtonContainer from './ButtonContainer.astro';

type Props = WithAttributes<ComponentProps<typeof ButtonContainer>>;

const { props } = Astro;
---

<ButtonContainer marginSize="sm" {...props}>
  <slot />
</ButtonContainer>
