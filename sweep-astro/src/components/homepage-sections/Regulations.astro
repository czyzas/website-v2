---
import { cmsStore } from '@/stores/cmsStore';
import { getStore } from '@/scripts/store';
import { parseSectionTitle } from '@/scripts/data-parsers/parseSectionTitle';
import Container from '../ui/Container.astro';
import SectionTitle from '../partials/SectionTitle.astro';
import LogosGrid from '../LogosGrid.astro';

const store = getStore(cmsStore);
const data = store.homepage!;
const acf = data.homepage!.regulations!;

const logos = acf.logos?.edges.filter(Boolean) ?? [];
---

<section class="regulations bg-sw-surface-background py-12 md:py-20">
  <Container>
    <SectionTitle
      withMargin
      {...parseSectionTitle(acf.sectionTitle?.selectSectionTitle)}
    />

    <LogosGrid columns={4} logos={logos} />
  </Container>
</section>
