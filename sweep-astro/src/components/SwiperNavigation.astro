---
import { Icon } from 'astro-icon/components';
import type { HTMLAttributes } from 'astro/types';
import { cn } from '@/scripts/cn';

type Props = HTMLAttributes<'div'>;

const { class: className } = Astro.props;
---

<div
  class={cn(
    'swiper-navigation',
    'flex divide-x overflow-hidden rounded-lg border border-sw-border bg-white text-black shadow-button-secondary',
    className
  )}
>
  <button
    type="button"
    class={cn(
      'swiper-navigation-prev',
      'flex items-center justify-center p-2.5 hover:bg-sw-surface-subdued',
      'transition-colors disabled:bg-sw-surface-background disabled:text-sw-text-disabled'
    )}
  >
    <Icon name="caret-left" />
  </button>
  <button
    type="button"
    class={cn(
      'swiper-navigation-next',
      'flex items-center justify-center p-2.5 hover:bg-sw-surface-subdued',
      'transition-colors disabled:bg-sw-surface-background disabled:text-sw-text-disabled'
    )}
  >
    <Icon name="caret-right" />
  </button>
</div>
