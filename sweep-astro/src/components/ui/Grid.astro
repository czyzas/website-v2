---
import type { HTMLAttributes } from 'astro/types';
import { cn } from '@/scripts/cn';

type Props = HTMLAttributes<'div'>;

const { class: className, ...rest } = Astro.props;
---

<div class={cn('grid gap-5 grid-cols-12', className)} {...rest}>
  <slot />
</div>
