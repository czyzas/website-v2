query PostsModuleList($LIMIT: Int, $CATEGORY_SLUG: [String], $LANG: String!) {
  posts(
    first: $LIMIT
    where: { language: $LANG, categorySlug: $CATEGORY_SLUG }
  ) {
    nodes {
      title
      categories {
        nodes {
          name
        }
      }
    }
  }
}
