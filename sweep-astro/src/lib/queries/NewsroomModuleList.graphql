query NewsroomModuleList(
  $LIMIT: Int
  $CATEGORY_SLUG: [String]
  $LANG: String!
) {
  newsroomItems(
    first: $LIMIT
    where: { language: $LANG, categorySlug: $CATEGORY_SLUG }
  ) {
    nodes {
      ...NewsroomListItem
    }
  }
}
