# Page
query DefaultPagesStaticPaths {
  pages(first: 100, where: { status: PUBLISH, language: "all" }) {
    nodes {
      template {
        templateName
      }
      databaseId
      slug
      uri
      languageCode
    }
  }
}
query SinglePageStaticPaths($STATIC_PATH_URI: String!) {
  page: pageByLang(uri: $STATIC_PATH_URI, lang: "en") {
    databaseId
    slug
    uri
    languageCode
    translations {
      databaseId
      slug
      uri
      languageCode
    }
  }
}

# Industry
query IndustryPagesStaticPaths {
  pages: industries(first: 100, where: { status: PUBLISH, language: "all" }) {
    nodes {
      databaseId
      slug
      uri
      languageCode
    }
  }
}

# Insights
query InsightsTagsStaticPaths {
  tags: insightsTags(first: 100, where: { language: "all", hideEmpty: true }) {
    nodes {
      databaseId
      slug
      uri
      languageCode
    }
  }
}
query InsightsPagesStaticPaths {
  pages: insights(first: 100, where: { status: PUBLISH, language: "all" }) {
    nodes {
      databaseId
      slug
      uri
      languageCode
    }
  }
}

# Newsroom
query NewsroomTagsStaticPaths {
  tags: newsroomTags(first: 100, where: { language: "all", hideEmpty: true }) {
    nodes {
      databaseId
      slug
      uri
      languageCode
    }
  }
}
query NewsroomPagesStaticPaths {
  pages: newsroomItems(
    first: 100
    where: { status: PUBLISH, language: "all" }
  ) {
    nodes {
      databaseId
      slug
      uri
      languageCode
    }
  }
}
