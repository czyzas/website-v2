query EventsModuleList($LIMIT: Int, $LANG: String!) {
  events(first: $LIMIT, where: { language: $LANG }) {
    nodes {
      databaseId
      title
      eventAcf {
        eventDate
      }
    }
  }
}
