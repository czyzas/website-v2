query EventsModuleList($LIMIT: Int, $CATEGORY_SLUG: [String], $LANG: String!) {
  events(
    where: {
      language: $LANG
      categorySlug: $CATEGORY_SLUG
      metaKey: "event_date"
      metaType: DATE
      orderby: { order: DESC, field: META_VALUE }
      pagination: { postsPerPage: $LIMIT, paged: 1 }
    }
  ) {
    nodes {
      ...EventListItem
    }
  }
}
