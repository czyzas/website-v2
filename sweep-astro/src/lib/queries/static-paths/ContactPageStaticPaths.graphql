query ContactPageStaticPaths {
  page: pageByLang(uri: "/contact", lang: "en") {
    databaseId
    slug
    uri
    languageCode
    translations {
      databaseId
      slug
      uri
      languageCode
    }
  }
}
