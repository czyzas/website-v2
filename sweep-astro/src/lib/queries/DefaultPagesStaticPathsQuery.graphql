query DefaultPagesStaticPaths {
  pages(first: 10000, where: { status: PUBLISH, language: "all" }) {
    nodes {
      template {
        templateName
      }
      databaseId
      slug
      uri
      languageCode
    }
  }
}
