query DefaultPagesStaticPaths {
  pages(where: { status: PUBLISH, language: "all" }) {
    nodes {
      template {
        templateName
      }
      databaseId
      slug
      uri
      languageCode
    }
  }
}
